[package]
name = "nova-eye-common"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Shared event types for the NovaVM observability subsystem"

[features]
default = ["std"]
std = []

[dependencies]
